{
  "dependencies": {
    "axios": "^1.7.9",
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7",
    "reflect-metadata": "^0.2.2",
    "tsyringe": "^4.8.0"
  },
  "devDependencies": {
    "inquirer": "^12.4.2",
    "prettier": "^3.5.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "format": "prettier --write .",
    "build": "tsc",
    "clean": "rimraf dist",
    "run": "node dist/index.js",
    "start": "npm run format && npm run clean && npm run build && npm run run"
  }
}
